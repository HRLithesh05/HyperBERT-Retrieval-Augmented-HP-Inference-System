Isotropy-Optimized Contrastive Learning for Semantic Course Recommendation
Ali Khreis, Anthony Nasr, Yusuf Hilal
School of Electrical and Computer Engineering
University of Ottawa
Ottawa, Canada
challenge is amplified for new students or those pursuing
interdisciplinary pathways, where little prior enrollment data
is available.
Abstract – This paper presents a semantic course
recommendation system for students using a self-supervised
Recommender systems have been widely used to
contrastive learning approach built upon BERT
personalize user experiences in domains such as e-commerce
(Bidirectional Encoder Representations from Transformers).
and online learning. Traditional course recommendation
Traditional BERT embeddings suffer from anisotropic
approaches typically rely on collaborative filtering, which
representation spaces, where course descriptions exhibit
infers preferences from historical enrollment data. Although
high cosine similarities regardless of semantic relevance. To
effective in some settings, these methods suffer from cold-start
address this limitation, we propose a contrastive learning
and long-tail problems, making them poorly suited for
framework with data augmentation and isotropy
academic environments where personalization based on
regularization that produces more discriminative
content is essential.
embeddings. Our system processes student text queries and
recommends Top-N relevant courses from a curated dataset
Content-based recommendation methods address
of over 500 engineering courses across multiple faculties.
these limitations by leveraging course descriptions directly.
Experimental results demonstrate that our fine-tuned model
Recent advances in natural language processing, particularly
achieves improved embedding separation and more accurate
pretrained language models such as BERT, enable contextual
course recommendations compared to vanilla BERT
semantic representations of text and allow both student interest
baselines.
statements and course descriptions to be embedded into a
shared vector space. However, prior work has shown that
vanilla BERT embeddings are highly anisotropic, causing
unrelated texts to exhibit high cosine similarity and limiting
their effectiveness for similarity-based retrieval tasks.
To overcome this limitation, we propose a self-
supervised contrastive learning framework built on top of a
pretrained BERT encoder for course recommendation. By
using text-based data augmentation and contrastive objectives,
the proposed method encourages semantically related inputs to
cluster together while pushing unrelated samples apart. An
Fig. 1. Model Specific flowchart additional isotropy regularization term is introduced to
improve the geometric structure of the embedding space. This
results in more discriminative representations that are well
I. INTRODUCTION
suited for cosine similarity–based ranking.
Course selection is an important yet challenging task
We apply the proposed approach to recommending
for engineering students, particularly at large institutions such
engineering courses at the University of Ottawa based on free-
as the University of Ottawa, where hundreds of courses are
form student interest statements. A curated dataset of
offered across multiple engineering disciplines. While
engineering course descriptions and synthetic student queries
program requirements and academic advising provide general
is constructed, and the system is evaluated using standard
guidance, students often struggle to identify courses that best
retrieval metrics and embedding isotropy analysis.
match their individual interests and career goals. This
Experimental results show that the proposed model
1
Code and datasets for this work are publicly available at:
https://github.com/nasrAnthony/Course-Recommendation-system/
significantly outperforms vanilla BERT embeddings in both augmentation combined with contrastive loss produces high-
recommendation accuracy and embedding quality. quality visual representations. Supervised contrastive learning
[9] extends this framework by leveraging label information to
The main contributions of this work are as follows: define positive pairs.
• A contrastive learning–based course
recommendation system operating on natural III. METHODOLOGY
language student input.
• A method to mitigate anisotropy in pretrained BERT In this paper, we build a course recommendation system by
embeddings using contrastive learning and isotropy mapping student interest statements and course information
regularization. into a shared semantic space and generating recommendations
• An empirical evaluation demonstrating improved using similarity-based course ranking. The model builds on a
recommendation performance over vanilla BERT pretrained BERT encoder (referred to as “Vanilla BERT” in
baselines. this paper) and extends it with a self-supervised contrastive
learning framework. Data augmentation is used during training
II. LITERATURE REVIEW to improve the model’s consistency and its ability to capture
semantic meaning, rather than relying on keyword matching or
rule-based filtering. After projecting the tokenized
A. Traditional Recommendation Approaches
embeddings into a lower-dimensional space, final course
recommendations are selected using cosine similarity between
Early course recommendation systems relied on
the student input and course embeddings.
collaborative filtering [1], which identifies patterns in student
enrollment histories to suggest courses. While effective when
sufficient data exists, these approaches suffer from cold-start
problems for new students and fail to capture course content
semantics.
Content-based methods using TF-IDF vectorization [2]
address some limitations by analyzing course descriptions
directly. However, these bag-of-words approaches cannot
capture semantic relationships between terms or handle
synonyms effectively.
B. Deep Learning in Recommendation Systems
The advent of deep learning has transformed
recommendation systems. Neural collaborative filtering [3]
combines matrix factorization with neural networks to learn Fig. 2. Overall system flowchart
non-linear user-item interactions. Knowledge graph
embeddings [4] incorporate structured relationships between E. Data Collection and Preprocessing
entities to improve recommendations. The data used in our application is divided into two main
components: course data and synthetic student data.
C. Pre-trained Language Models
1) Course Data Collection:
BERT [5] revolutionized natural language processing by Only engineering courses were used in our application, and
learning contextual word representations through masked course information was collected from the University of
language modeling. Sentence-BERT [6] adapted BERT for Ottawa course catalogue. For each course, the course code,
sentence-level embeddings using siamese networks. However, title, description, components, and prerequisites were
research has shown that vanilla BERT embeddings occupy a collected, but only the text-based fields were combined to
narrow cone in the embedding space (anisotropy), resulting in produce a single semantic representation of each course.
artificially high cosine similarities between unrelated texts [7].
TABLE I
D. Contrastive Learning SAMPLE CLEANED COURSE DATA
Faculty Code TextforBERT
Contrastive learning has emerged as a powerful self- ELG 2136 “electronics i. physics of
supervised technique for learning discriminative semiconductors...”
representations. SimCLR [8] demonstrated that data
2
MCG 3110 “heat transfer. fundamental principles
of...”
CHG 8301 “renewable fuels. the production and
sustainability of...”
Text partially shown to account for space
Courses with sufficient descriptive content were kept in the
dataset to ensure that only meaningful semantics were used.
Since the University of Ottawa offers courses in English and
French, only English courses were kept. This resulted in a total
of 512 relevant engineering courses.
2) Synthetic Student Statement Generation
Student inputs were generated as common statements that
could be used to express a student’s liking of a specific subject.
The structure of these inputs was made to vary the length and
style of wording that might be used by students when naturally
describing their interests. Therefore, the synthetic student data
Fig. 3. Data augmentation process chart
would look something like: “I enjoy learning about the
processing of electrical power.”
The augmentation process follows the pipeline shown in Fig.
Each student input was associated with a single course
3. For each course data sample, a synthetic data retriever first
considered to be a positive match. This tag would be used later
checks whether an associated student statement exists. If
during data augmentation and when evaluating the model. A
available, the associated student statement is used as one view,
total of 600 student statements were generated, and an 80/20
while the second view is a heavily augmented version of the
training/testing split was done.
course data. If no associated student statement exists, both
views are created through independent augmentations of the
TABLE II
same course description. This design ensures that each training
SAMPLE SYNTHETIC STUDENT DATA
sample produces a valid positive pair corresponding to the
Student Text Liked Courses
same underlying course.
i enjoy computer system design CEG 3156; ELG 5383
i like mechatronics MCG 4136; ELG 6393 The following text-based augmentation methods were applied
to course descriptions. Each augmentation operation was
i am interested in courses like CVG 7116
applied with different probabilities depending on whether a
fundamentals of geomechanics
light or heavy augmentation was selected.
• Word Deletion
• Synonym Replacement
3) Text Cleaning and Normalization
• Word Insertion
The final part of the preprocessing was to ensure that all the
• Word Swapping
text was normalized and clean. This step simply included
lower casing and the removal of noninformative characters, as
Text augmentation was implemented using the NLTK library,
well as the normalization of white space. Overall, minimal
with WordNet used for synonym-based transformations.
filtering was applied to preserve the semantic structure. Both
Augmentation was applied exclusively to course texts and only
the course data and the synthetic student statements were
during training. Student statements were never modified and
processed through this cleaning pipeline to ensure effective
were used only when available to form positive pairs. This
tokenization by the pretrained BERT encoder.
approach enables robust learning of course representations
while incorporating real student intent when available,
F. Augmentation
improving generalization in similarity-based
recommendations.
Since our approach is based on a self-supervised contrastive
learning framework, data augmentation is a required
G. Model Base
component of the training process. The primary motivation for
this step is to provide the model with reliable positive pairs for
Our model is built on a pretrained BERT backbone and is
each course, and to expand our effective data set, given that
extended with a small projection head to produce embeddings
only 512 courses and 480 training student statements were
suitable for similarity-based course recommendations. BERT
available. Without data augmentation, the model wouldn't be
processes input text by first splitting it into tokens (text units
able to learn, and semantic understanding would be limited.
3
produced by the tokenizer), where each token is mapped to a the same label are treated as positives. The NT-Xent loss
numerical vector whose representation depends on the function is defined as:
surrounding context. This allows the model to capture
semantic meaning, not just individual words. The encoder and
projection head were implemented using PyTorch, with
pretrained BERT weights loaded through the Hugging Face
Transformers library.
[1]
Since the recommendation pipeline requires a single vector per where:
input, masked mean pooling is applied over the final hidden • 𝑍 ∈ ℝ2Β×𝐷 is the matrix of all embeddings in the
states, which are the vector output of the final BERT layer. batch
This operation averages the token vectors across the sequence • 𝑍 is the current embedding (one row of Z)
𝑎
while ignoring padding tokens. The attention mask (a binary • Ρ(𝑎) is the set of positive samples for the current
indicator for valid tokens) ensures that only meaningful tokens embedding, defined as all embeddings sharing the
contribute to the pooled representation. The result is a fixed same label and excluding the embedding itself.
size embedding that represents the overall semantic content of • sim(A, B) denotes cosine similarity where:
the input and remains robust to variations in input length.
[2]
To adapt the encoder for contrastive training, the pooled
• 𝜏 is a temperature parameter controlling the
embedding is passed through a projection head composed of
sharpness of the similarity distribution.
two fully connected layers with a ReLU activation in between.
• Β is the batch size, and D is the embedding
The first layer operates on the original 768-dimensional pooled
dimension.
BERT representation and introduces nonlinearity, allowing the
model to learn weights for the semantic features extracted by
The loss is computed for all the embeddings in a batch and
BERT. The second layer projects this representation onto a
averaged across that batch. The temperature parameter was
lower-dimensional embedding space (256 dimensions in our
tuned empirically to provide improved isotropy without
implementation) and is used for similarity-based comparison.
The resulting embedding 𝑧 ∈ ℝ256 is L2 normalized so that sacrificing accuracy of the model.
similarity comparisons depend only on vector direction rather
In addition to the contrastive loss, an isotropy loss term is
than magnitude. L2 normalization ensures that both the
applied to the embeddings. This loss encourages zero-mean
contrastive loss during training and cosine similarity during
and unit-variance features across the batch, meaning a more
inference depend only on embedding direction rather than
uniform distribution in the embedding space. The final training
magnitude.
loss is given by:
H. Loss Function
Where 𝜆 controls the contribution of the isotropy loss.
Training is guided by a contrastive loss that enforces semantic
similarity in the learned embedding space. The loss operates The model was trained using the AdamW optimizer with a
on embeddings produced from the two views generated by the linear learning rate schedule and warm-up. Training was
encoder. Although the overall framework follows a self- performed for a fixed number of epochs using mini-batches,
supervised contrastive learning approach based on view and gradient clipping was applied to improve stability. All
generation and data augmentation, the training uses a hyperparameters were selected empirically and kept fixed
supervised contrastive loss where positives are defined using across experiments.
available labels.
IV. RESULTS
For a batch of size 𝐵 , the encoder produces two L2-normalized
embeddings per sample, corresponding to the two views used We begin by illustrating the recommendation process using a
in contrastive learning. These embeddings are concatenated to sample student input. Given a student interest statement, the
form a single matrix, 𝑍 ∈ ℝ2𝐵×𝐷, which serves as the input to trained model maps the input into the same embedding space
the contrastive loss. Similarity is computed between all pairs as the course representations. The recommendations are then
of embeddings within the batch, allowing the loss to use the generated by ranking courses using cosine similarity. For the
predefined positive samples while treating the remaining example shown, the top-N (top-5 in our implementation)
embeddings in the batch as negatives. While based on the recommended courses align well with the student’s stated
standard NT-Xent formula, the loss is extended to a interests, despite differences in wording between the input and
supervised, multi-positive setting in which all samples sharing course descriptions.
4
effectiveness of our contrastive, self-supervised approach.
Also, among the evaluated temperature values, a temperature
of τ = 0.05 yields the best overall performance, indicating an
effective balance between positive alignment and negative
separation.
TABLE III
METRICS
Vanilla Our Model
Metric
BERT τ = 0.2 τ = 0.05 τ = 0.01
Hit Rate 0.033 0.833 0.917 0.925
Fig. 4. Sample output with input example
F1 Score 0.021 0.654 0.725 0.733
A two–dimensional UMAP (Uniform Manifold
MRR 0.014 0.658 0.733 0.76
Approximation and Projection) illustrates the model’s
IsoScore 0.818 0.031 0.065 0.082
behavior, where the student’s embedding is close to its top-
ranked courses. This provides an intuitive visualization of how
recommendations are produced in the learned semantic space. J. Embedding Structure and Isotropy
The performance gains observed in section A are further
supported by analyzing the geometric structure of the learned
embedding space. Fig. 6. compares UMAP projections of
course embeddings produced by Vanilla BERT and the
contrastively trained model. Vanilla BERT embeddings
exhibit strong anisotropy, with many courses clustered closely
together regardless of semantic differences. In contrast, the
trained model produces a more evenly distributed embedding
space with clearer separation between course groups, enabling
more meaningful similarity comparisons.
Fig. 5. UMAP output showcasing all courses mapped for the
fine-tuned BERT model
I. Recommendation Accuracy Evaluation Fig. 6. Comparison of UMAP output for vanilla BERT and
fine-tuned BERT
Evaluation of the recommender system was performed on a
test student input dataset, where statements were embedded Table IV reports the mean and standard deviation of cosine
and matched against the full set of course embeddings. similarity values between randomly sampled course pairs.
Performance was measured using Hit Rate, F1 score, and Vanilla BERT embeddings show a high mean similarity with
Mean Reciprocal Rank (MRR), which evaluate the top N low variance, indicating limited discriminability. After
retrieval accuracy, ranking quality, and early relevance, contrastive training, the mean similarity decreases and
respectively. In addition to these common recommender variance increases, reflecting improved separation between
metrics, embedding isotropy was analyzed separately using the unrelated courses. This trend is further confirmed by the
IsoScore [10] metric to assess the geometric quality of the IsoScore metric, which reports a higher isotropy score for the
learned representation space. trained model.
TABLE IV
Table III reports the performance of Vanilla BERT and the COURSE COSINE SIMILARITY DISTRIBUTION
proposed contrastive model. Vanilla BERT exhibits low
Cosine Vanilla Our Model
retrieval performance across all metrics, indicating that the
pretrained embeddings are weak for a semantic course similarity BERT τ = 0.2 τ = 0.05 τ = 0.01
recommendation application. In contrast, our model achieves Mean 0.818 0.016 0.115 0.469
substantially higher scores across all metrics, highlighting the
5
Standard issue, we introduced contrastive learning with text-based data
0.054 0.271 0.19 0.135
deviation augmentation and incorporated an isotropy regularization term
to improve the geometric structure of the embedding space.
The effect of the temperature parameter on embedding Experimental results demonstrate that the proposed model
structure is illustrated in Fig. 7. Higher temperature values produces more discriminative and evenly distributed
reduce embedding separation, while excessively low embeddings, leading to substantial improvements in
temperatures produce overly compact or unstable recommendation accuracy compared to vanilla BERT
representations. A temperature of τ = 0.05 produces the most baselines.
balanced structure, which aligns with the highest
recommendation performance reported in Table III. Quantitative evaluation using Hit Rate, F1 score, and Mean
Reciprocal Rank confirms that the contrastively trained model
significantly outperforms pretrained BERT embeddings across
all retrieval metrics. Additional analysis using IsoScore and
cosine similarity distributions further validates that the learned
embedding space exhibits improved isotropy and separation
between unrelated courses. Visualization using UMAP
projections provides intuitive evidence of these improvements,
showing clearer clustering and alignment between student
inputs and relevant courses.
While the proposed system demonstrates strong performance,
several directions for future work remain. Incorporating real
student interaction data could enable personalized
recommendations over time, while extending the dataset
beyond engineering courses would allow cross-faculty
recommendations. Additionally, exploring larger language
models or interactive feedback mechanisms may further
improve recommendation quality and usability. Overall, this
work highlights the effectiveness of contrastive learning for
semantic course recommendations and demonstrates its
potential as a practical decision-support tool in academic
advising systems.
VI. REFERENCES
Fig. 7. Collection of all UMAP outputs for models trained
with varying parameters [1] B. Sarwar, G. Karypis, J. Konstan, and J. Reidl, “Item-based
collaborative filtering recommendation algorithms,” Proceedings of
the tenth international conference on World Wide Web - WWW ’01,
pp. 285–295, May 2001, doi: https://doi.org/10.1145/371920.372071.
V. CONCLUSION
[2] I. Lumintu, “Content-Based Recommendation Engine Using Term
In this work, we presented a course recommendation system Frequency-Inverse Document Frequency Vectorization and Cosine
for engineering students based on a self-supervised contrastive Similarity: A Case Study,” Oct. 2023, doi:
learning framework built on top of a pretrained BERT encoder. https://doi.org/10.1109/itis59651.2023.10420137.
The proposed approach maps free-form student interest
[3] X. He, L. Liao, H. Zhang, L. Nie, X. Hu, and T.-S. Chua, “Neural
statements and course descriptions into a shared semantic
Collaborative Filtering,” Proceedings of the 26th International
embedding space and generates recommendations using
Conference on World Wide Web - WWW ’17, pp. 173–182, 2017, doi:
cosine similarity–based ranking. By focusing on semantic
https://doi.org/10.1145/3038912.3052569.
understanding rather than historical enrollment data, the
system effectively addresses common challenges in academic [4] X. Wang, X. He, Y. Cao, M. Liu, and T.-S. Chua, “KGAT,”
recommendation, including cold-start scenarios and niche Proceedings of the 25th ACM SIGKDD International Conference on
student interests. Knowledge Discovery & Data Mining, Jul. 2019, doi:
https://doi.org/10.1145/3292500.3330989.
A key limitation of vanilla BERT embeddings—anisotropy in
the representation space—was identified as a major obstacle
[5] J. Devlin, M.-W. Chang, K. Lee, and K. Toutanova, “BERT: Pre-
for similarity-based course recommendation. To mitigate this
training of Deep Bidirectional Transformers for Language
6
Understanding,” ArXiv, Oct. 11, 2018.
https://arxiv.org/abs/1810.04805
[6] N. A. Alnajem, M. Binkhonain, and M. Shamim Hossain,
“Siamese Neural Networks Method for Semantic Requirements
Similarity Detection,” IEEE Access, vol. 12, pp. 140932–140947,
2024, doi: https://doi.org/10.1109/access.2024.3469636.
[7] K. Zeng and I. Paik, “Semantic Service Clustering With
Lightweight BERT-Based Service Embedding Using Invocation
Sequences,” IEEE Access, vol. 9, pp. 54298–54309, 2021, doi:
https://doi.org/10.1109/access.2021.3069509.
[8] Youness Moukafih, A. Ghanem, K. Abidi, N. Sbihi, Mounir
Ghogho, and Kamel Smaïli, “SimSCL: A Simple Fully-Supervised
Contrastive Learning Framework for Text Representation,” Lecture
notes in computer science, pp. 728–738, Jan. 2022, doi:
https://doi.org/10.1007/978-3-030-97546-3_59.
[9] C. Wu, H. Yuan, P. Zhao, J. Qu, V. S. Sheng, and G. Liu, “Dual-
Supervised Contrastive Learning for Bundle Recommendation,”
IEEE Transactions on Computational Social Systems, vol. 11, no. 3,
pp. 3955–3965, Dec. 2023, doi:
https://doi.org/10.1109/tcss.2023.3331255.
[10] W. Rudman, N. Gillman, T. Rayne, and C. Eickhoff, “IsoScore:
Measuring the Uniformity of Embedding Space Utilization,” 2022.
Available: https://aclanthology.org/2022.findings-acl.262.pdf
7